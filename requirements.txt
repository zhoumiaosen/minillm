# Install a CUDA-compatible PyTorch build separately before this file.
datasets==3.6.0
numpy==1.26.4
transformers==4.57.6
requests
# Needed by the upstream InternLM reward model's custom implementation.
einops==0.8.1
sentencepiece==0.2.0
